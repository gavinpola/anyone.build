// Placeholder. scripts/vercel-build.mjs overwrites this at build time with the built dist/index.html so the
// share page can serve the app with preview tags injected. Empty here means "fetch /index.html instead".
export const INDEX_HTML = "";
